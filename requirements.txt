# Targeted Adaptive Design — dependencies
# Versions reflect the environment used for the paper runs (2022).
# gpytorch 1.6.1 is the key pin; it fixes the compatible torch/numpy range.

gpytorch==1.6.1
torch>=1.10,<1.12
numpy>=1.21,<1.24
scipy>=1.7
matplotlib>=3.4
seaborn>=0.11

# Notebook environment
jupyter
ipython
